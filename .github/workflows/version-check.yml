name: Check Version Tag

on:
  pull_request:

jobs:
  tag_check:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Check package version
        run: |
          ls -la
          cd google-surfer
          VERSION=$(grep "version" pyproject.toml | cut -d'"' -f 2)
          if git show-ref --tags --verify --quiet "refs/tags/${VERSION}"; then
            echo "Package version ${TAG} already exists."
            exit 1
          else
            echo "Package version ${TAG} does not exist."
          fi
  build:
    runs-on: ubuntu-22.04
    needs: [build]
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.9"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build
      - name: Build package
        run: |
          python -m build
          ls -la
